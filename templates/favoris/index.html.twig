{% extends 'base.html.twig' %}

{% block title %}Mes Favoris{% endblock %}

{% block content %}
    <h1>Mes favoris</h1>
    Retrouvez l'ensemble des produits que vous avez ajouté à votre favoris.
    {% if favoris| length > 0 %}

    <table class="table mt-3">
    <thead>
    <tr>
    <th scope="col">Référence</th>
    <th scope="col"></th>
    <th scope="col">Prix unitaire</th>
    <th scope="col"></th>
    </tr>
    </thead>


    <tbody>
    {% for product in favoris %}
       <tr>
    <th > <img src="/uploads/{{product.product.illustration}}" alt="{{ product.product.name}}" height="75px" ><br>
    </th>
    <td>
    {{ product.product.name}}<br>
    <small>{{ product.product.subtitle}}</small>
    </td>
            
          
           
            <th >{{ (product.product.price / 100)|number_format(2, ',','.')}} €</th> 
    <td>
            <a href={{ path('delete_my_product', {id : product.product.id})}}>
                    <img src={{asset('assets/img/poubelle.png')}} height="18px" alt="Supprime mon produit" >
            </a>
    </td>
    </tr>
    {% endfor %}
    </tbody>

    </table>

    <div class="text-right mb-5">
    <b>Nombre de réfernce :</b> {{favoris |length}} <br/>
    </div>
    {% else %}
    <hr>
    <p><b> Votre page favoris est vide </b></p>
    {% endif %}
{% endblock %}
